name: test

on:
  pull_request:
  push:
    branches:
      - v1

jobs:
  go:
    uses: ./.github/workflows/go.yaml
    with:
      go-version: 1.20.3
      golangci-lint-version: v1.52.2

  manifest:
    uses: ./.github/workflows/manifest.yaml
