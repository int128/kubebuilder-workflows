name: test

on:
  pull_request:
  push:
    branches:
      - v1

jobs:
  go:
    uses: ./.github/workflows/go.yaml
    with:
      go-version: 1.20.6
      golangci-lint-version: v1.53.3

  manifest:
    uses: ./.github/workflows/manifest.yaml
